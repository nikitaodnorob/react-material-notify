import { FC } from 'react';
import { NotificationType } from './types';
export declare const Notification: FC<NotificationPropsType>;
declare type NotificationPropsType = {
    notification: NotificationType;
};
export {};
